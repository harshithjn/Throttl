apiVersion: v1
kind: ConfigMap
metadata:
  name: throttl-config
  namespace: throttl
data:
  REDIS_URL: "redis:6379"
  DATABASE_URL: "postgres://throttl:throttl@postgres:5432/throttl?sslmode=disable"
  PORT: "8080"
  THROTTL_ENV: "production"
  PROMETHEUS_URL: "http://prometheus:9090"
  GRAFANA_URL: "http://grafana:3000"

---
apiVersion: v1
kind: Secret
metadata:
  name: throttl-secrets
  namespace: throttl
type: Opaque
data:
  # Base64 encoded values
  POSTGRES_PASSWORD: dGhyb3R0bA== # throttl
  GRAFANA_ADMIN_PASSWORD: YWRtaW4= # admin
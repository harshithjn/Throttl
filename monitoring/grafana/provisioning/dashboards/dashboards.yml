apiVersion: 1

providers:
  - name: 'throttl-dashboards'
    orgId: 1
    folder: 'Throttl'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards